# php-AP

Github sur cmd:
git pull => server -> local
git checkout => change de branche
git branch => afficher les branches
git add fichier => ajouter un fichier
git commit  -m nom_fichier.type => valide
git push => local -> server
git status => etat changement
git log => lister commit


PHP:
include('nom_page.type') => récupe des infos de la page